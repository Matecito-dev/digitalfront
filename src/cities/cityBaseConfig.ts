/**
 * Base city configuration — values that apply to a city before any buildings.
 * All configurable, zero hardcoded gameplay numbers.
 */
export const CITY_BASE_CONFIG = {
  baseStorage: {
    maxGold: 1000,
    maxWood: 1000,
    maxStone: 500,
    maxFood: 500,
  },
};
