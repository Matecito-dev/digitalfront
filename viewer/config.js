/** URL del servidor MMO — sobrescribible desde Ajustes (localStorage) o DF_SERVER_URL en build. */
window.DF_CONFIG = {
  apiBase: "",
  wsPath: "/api/sim/ws",
};

/** @deprecated alias */
window.VELIS_CONFIG = window.DF_CONFIG;
